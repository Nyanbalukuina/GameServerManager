# ARK Server Manager 開発手順

## 1. 目的

GameServerManager（GSM）に、ARKの専用サーバーを構築・管理する機能を追加する。

この開発では、完成させることだけでなく、Kotlinを学びながら自分の手でコーディングすることを重視する。

## 2. 開発の進め方

- 実際のコード入力とファイル編集は、原則として開発者本人が行う。
- AIは、実装する機能を小さなタスクに分割し、順番に案内する。
- AIは、貼り付けるコード、貼り付け先のファイル、貼り付け位置を明確に示す。
- 開発者は、案内されたコードをコピーして指定箇所へ貼り付ける。
- 貼り付け後は、ビルドやテストの結果を確認してから次のタスクへ進む。
- エラーが発生した場合は、次の機能へ進まず、そのタスク内で原因を解消する。

AIが開発者の許可なくコードを直接編集することはしない。コードレビュー、エラー調査、既存コードの確認など、読み取りだけの作業は必要に応じて行う。

## 3. タスクの分け方

一度に多くの変更を行わず、動作を確認しやすい単位に分割する。

- 原則として、1つのAPIまたは1つの機能を1タスクとする。
- 1つのAPIが複数の関数で構成される場合は、それらをまとめて1タスクとしてよい。
- 複数のファイルを変更する場合も、同じ機能を完成させるために必要な範囲だけに限定する。
- 共通化やリファクタリングは、ARKとPalworldで実際に共通すると確認できた処理だけを対象にする。
- 将来使うかもしれないという理由だけで、先に共通化しない。

## 4. AIによる案内形式

各タスクでは、AIは次の順番で案内する。

1. 今回作る機能と、その目的
2. 変更または作成するファイルのパス
3. コードを貼り付ける正確な位置
4. 貼り付けるコード全体
5. コードの主な処理内容
6. 実行するビルドまたはテストコマンド
7. 成功時に表示される結果
8. 次のタスクへ進む前に確認する項目

既存コードの一部を置き換える場合は、次のいずれかを明記する。

- ファイル全体を置き換える
- 指定したクラスまたは関数だけを置き換える
- 指定した行の直前または直後へ追加する

「適切な場所へ追加する」など、Kotlinの知識が必要になる曖昧な指示は使用しない。

## 5. コードの説明

- 提示するコードには、学習に必要な範囲で短いコメントを付ける。
- コメントは、処理の目的を1行程度で説明する。
- Kotlin特有の記法を初めて使用する場合は、コードの後に簡潔な説明を付ける。
- 自明な処理へ過剰なコメントを追加しない。
- 使用していない変数、不要な処理、将来用だけのコードは残さない。

## 6. 共通処理とゲーム専用処理の方針

共通処理とARK専用処理は分離する。ただし、共通化によって処理の流れが分かりにくくなる場合は、無理に共通化しない。

### 共通処理の候補

- SteamCMDの配置、更新、実行
- SteamCMDの同時実行を防ぐ排他制御
- ポートの入力確認と使用状況の確認
- Windows Firewall設定の依頼
- サーバープロセスの起動、停止、状態確認
- ワールド構築の進捗表示に使用する共通データ

### ARK専用処理

- Steam App IDなどのARK固有情報
- ARK専用サーバーの配置先と実行ファイル
- ARK固有の起動引数
- `GameUserSettings.ini`と`Game.ini`の読み書き
- ARK固有の終了方法、保存方法、バックアップ仕様

ARK専用のクラスや処理は、原則としてARK用フォルダ以下へ配置する。

## 7. 開発予定の順序

次の順序を基本とする。各項目は、さらに小さなタスクへ分割して進める。

1. 既存のPalworld構築処理と共通処理を確認する
2. ARK用のデータモデルと構築APIの最小構成を作る
3. 共通のSteamCMDを使ってARK専用サーバーをインストールする
4. インストール先とサーバー情報をGSMへ登録する
5. ARKの起動コマンドを組み立てる
6. ARK専用サーバーの起動、停止、状態確認を実装する
7. ポート確認とWindows Firewall設定を組み込む
8. ARKの設定ファイルを作成する
9. 現在の設定を表示するAPIと画面を作る
10. 停止中だけ設定を編集できるAPIと画面を作る
11. デモモードへ対応する
12. 各機能のテストと本番環境での検証手順を整える

順序を変更したほうが安全な場合は、変更理由を確認してから進める。

## 8. 各タスクの完了条件

次の条件をすべて満たしたら、そのタスクを完了とする。

- 指定されたコードの入力が完了している。
- IDE上に新しいコンパイルエラーがない。
- 対象のビルドまたはテストが成功している。
- 既存のPalworld機能を壊していない。
- 開発者がコードの大まかな役割を確認できている。

## 9. エラー発生時の進め方

エラーが発生した場合、開発者は次の情報をAIへ渡す。

- 実行したコマンド
- エラーメッセージ全文
- IDEでエラーになっているファイル名と行番号
- 直前に貼り付けたコード

AIは原因を説明したうえで、修正するファイル、修正位置、置き換えるコードを明確に案内する。複数の修正をまとめず、原因ごとに確認しながら進める。

## 10. 最初の作業

最初に既存のPalworld構築処理を確認し、以下を分類する。

- そのまま再利用できる共通処理
- ARK向けに値を差し替えて利用できる処理
- ARK専用として新しく作る処理

分類結果を確認した後、ARK構築APIの最小構成から実装を開始する。

