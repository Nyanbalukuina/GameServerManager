# GameServerManager 開発手順書

## 1. 目的

GameServerManagerは、コマンド操作に不慣れな利用者でも、ブラウザからゲーム専用サーバーを安全に構築・運用できるようにするOSSである。

初期版では、Windows上へPalworld Dedicated Serverを新規構築し、そのまま管理できる機能を作る。

既に構築されているゲームサーバーの取り込みは、新規構築と基本的な管理機能が完成した後に対応する。

## 2. 初期版の対象

### 対象

- 単一のWindowsマシン
- Palworld Dedicated Server
- Windowsネイティブ実行
- ローカルまたはTailscale経由のブラウザ操作
- 単一管理者

### 初期版に含めないもの

- ARK対応
- Linux対応
- Docker対応
- 複数マシン管理
- 既存サーバーの自動検出
- 任意コマンド実行
- インターネットへの管理画面公開

## 3. 技術構成

| 分類 | 採用技術 |
|---|---|
| バックエンド言語 | Kotlin |
| フロントエンド言語 | TypeScript |
| 実行環境 | Java 21 |
| バックエンド | Spring Boot |
| バックエンドビルド | Gradle Kotlin DSL |
| Web画面 | React |
| フロントエンドビルド | Vite |
| バックエンドテスト | JUnit 5、Spring Boot Test、MockMvc |
| フロントエンドテスト | Vitest、Testing Library |
| データベース | SQLiteを後から導入 |
| DBマイグレーション | Flywayを後から導入 |
| Windows操作 | PowerShell |
| ゲーム導入・更新 | SteamCMD |
| ゲーム操作 | RCON |

## 4. 開発環境

必要なツール：

- JDK 21
- Node.js 24
- npm
- IntelliJ IDEA
- Git

Gradle本体の個別インストールは不要である。プロジェクトに含まれるGradle Wrapperを使用する。

### バックエンドの起動

```powershell
cd backend
.\gradlew.bat bootRun
```

バックエンドAPIは以下で起動する。

```text
http://localhost:8080
```

`bootRun`はWebサーバーを実行し続けるため、Gradleの進捗表示は100%にならない。停止するときはターミナルで`Ctrl+C`を押す。

### フロントエンドの起動

別のターミナルで以下を実行する。

```powershell
cd frontend
npm.cmd run dev
```

画面は以下で起動する。

```text
http://localhost:5173
```

開発中の`/api`通信はViteからSpring Bootへ転送する。

### バックエンドテスト

```powershell
cd backend
.\gradlew.bat test
```

### フロントエンドテスト

```powershell
cd frontend
npm.cmd test
```

### プロキシ環境

Gradleが外部リポジトリへ接続できない環境では、Windowsユーザー環境変数`GRADLE_OPTS`へHTTP・HTTPSプロキシを設定する。

プロキシのアドレス、認証情報、端末固有設定はリポジトリへ保存しない。

## 5. 開発方針

- 小さな機能単位で実装する
- 画面、入力検証、処理、テストを一つの単位として完成させる
- ファイル作成や外部コマンド実行の前に確認画面を設ける
- 外部コマンドへ利用者の入力を直接連結しない
- 実行可能な操作をアプリ側で限定する
- ゲーム固有処理とOS固有処理を分離する
- 失敗時に途中状態と原因を確認できるようにする
- 破壊的な操作の前にバックアップを作成する
- パスワードやトークンを平文で保存しない
- Webアプリ全体を管理者権限で常時実行しない

## 6. 新規サーバー構築の流れ

```text
構築情報を入力
→ 入力内容を検証
→ 構築計画を確認
→ SteamCMDを準備
→ Palworldサーバーをインストール
→ 初期設定を生成
→ サーバーを起動
→ 起動とポート待受を確認
→ 管理対象として登録
```

構築処理は一括した不透明なコマンドにせず、各段階の開始、成功、失敗を記録する。

## 7. 開発順

### フェーズ1：構築計画

1. 新規構築フォームを作成する
2. 入力値を検証する
3. 構築計画の確認画面を作成する
4. フォームと検証のテストを追加する

入力項目：

- サーバー名
- インストール先
- SteamCMDの保存先
- ゲームポート
- RCONポート
- 最大プレイヤー数
- サーバーパスワード
- 管理者パスワード

この段階ではファイル作成、SteamCMD実行、Firewall変更を行わない。

### フェーズ2：事前検証

1. インストール先の妥当性を確認する
2. パスが許可された範囲内か確認する
3. 必要な空き容量を確認する
4. 使用予定ポートの重複を確認する
5. SteamCMDの既存インストールを確認する
6. 検証結果を画面へ表示する

### フェーズ3：SteamCMD準備

1. SteamCMDの公式配布元を固定する
2. ダウンロード先を制限する
3. ダウンロードしたファイルを検証する
4. 専用フォルダーへ展開する
5. 実行結果とエラーを記録する

### フェーズ4：Palworldインストール

1. Steam App IDをアプリ側で固定する
2. SteamCMDの引数をアプリ側で生成する
3. インストール処理を実行する
4. 終了コードとログを確認する
5. 必要な実行ファイルの存在を確認する
6. 失敗時に再実行できるようにする

### フェーズ5：初期設定

1. Palworldの初期設定を生成する
2. ポートと最大プレイヤー数を反映する
3. パスワードを安全に扱う
4. 生成前に既存ファイルを確認する
5. 上書き前にバックアップを作成する

### フェーズ6：起動と状態確認

1. Palworldサーバーを起動する
2. プロセス状態を確認する
3. UDPポートの待受を確認する
4. 起動ログを表示する
5. タイムアウトと異常終了を処理する

### フェーズ7：管理機能

1. 状態表示
2. 安全な起動
3. RCONによる保存と停止
4. 再起動
5. セーブデータのバックアップ
6. SteamCMDによる更新
7. 操作履歴

### フェーズ8：OS連携

1. Windows Firewall設定
2. Windows起動時の自動起動
3. 定期バックアップ
4. スケジュール実行

管理者権限が必要な処理は通常処理から分離し、限定された操作だけを許可する。

### フェーズ9：認証とデータ保存

1. SQLiteを導入する
2. Flywayを導入する
3. 管理対象サーバーを保存する
4. Spring Securityを導入する
5. 管理者認証を追加する
6. 操作履歴を保存する

認証なしでTailscale外部からアクセスできる状態にしない。

### フェーズ10：既存サーバー取り込み

1. 既存サーバーの手動登録画面を作成する
2. 実行ファイルと設定ファイルを検証する
3. セーブデータの場所を確認する
4. 既存の起動方法を明示的に選択させる
5. 読み取り専用の確認後に管理対象へ登録する

初期段階ではPC全体を走査する自動検出を実装しない。

## 8. コード構成の方針

```text
GameServerManager/
├─ backend/
│  └─ src/
│     ├─ web/api/
│     ├─ application/
│     ├─ domain/
│     └─ infrastructure/
└─ frontend/
   └─ src/
      ├─ api/
      ├─ types/
      └─ components/
```

ReactはSpring BootのREST APIだけを呼び出す。ReactからPowerShellやSteamCMDを直接実行しない。

## 9. 1機能の開発手順

1. 対象機能と対象外を決める
2. 正常系と主な異常系を決める
3. 入出力モデルを作る
4. バックエンドのドメイン、アプリケーション処理、REST APIを実装する
5. Reactの画面とAPI通信を実装する
6. バックエンドとフロントエンドの自動テストを追加する
7. `backend`で`.\gradlew.bat test`を実行する
8. `npm.cmd test`を`frontend`で実行する
9. バックエンドとフロントエンドを起動して画面を確認する
10. Git差分を確認する
11. 1機能単位でコミットする

## 10. 完了条件

各機能は以下を満たした時点で完了とする。

- 正常系が動作する
- 想定する入力エラーを画面へ表示できる
- 外部処理の失敗理由を確認できる
- 必要な自動テストが成功する
- 秘密情報をログへ出力しない
- 対象外のファイルを変更しない
- 不要なコードや未使用処理が残っていない
- 利用者がブラウザから操作方法を理解できる

## 11. 最初に実装する機能

最初の機能は以下とする。

```text
新規構築フォーム
→ 入力値検証
→ 構築計画の確認画面
```

この機能では外部コマンドを実行せず、入力された内容から安全な構築計画を生成して表示する。
