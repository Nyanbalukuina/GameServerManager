# ARK: Survival Ascended専用サーバー構築

## 1. 開発目標

GSMからARK: Survival Ascended（以下、ASA）の専用サーバーを新規構築し、ASAクライアントからワールドへログインして実際に遊べる状態にする。

最初の完成版では、ASAサーバーの管理機能をすべて揃えることよりも、次の一連の流れを確実に成功させることを優先する。

```text
GSMで構築情報を入力する
  → SteamCMDでASA専用サーバーをインストールする
  → 最低限のワールド設定を保存する
  → Windows Firewallを設定する
  → ASA専用サーバーを起動する
  → クライアントからログインする
  → ワールド内でプレイする
```

## 2. 対象製品

この開発では、次の製品だけを対象とする。

- ARK: Survival Ascended（ASA）

ARK: Survival Evolved（ASE）は対象外とする。Steam App ID、実行ファイル、必要なポート、起動引数、設定項目はASAの仕様だけを基準に実装する。

## 3. 最小構成に含める機能

### 3.1 構築画面

GSMの画面から、ASAサーバーの構築に最低限必要な情報を入力できる。

- サーバー名
- マップ
- セッションまたはワールド名
- 最大プレイヤー数
- サーバーパスワード
- 管理者パスワード
- プレイヤー接続用ポート
- Queryポートなど、ASAで起動に必要なポート
- 接続を許可するネットワーク範囲
- インストール先

入力項目は、ASAの専用サーバー仕様を確認してから確定する。

### 3.2 事前確認

構築を始める前に、少なくとも次の項目を確認する。

- Windows上で動作していること
- インストール先がGSMの管理対象として許可されていること
- 必要な空き容量があること
- SteamCMDを準備または再利用できること
- 指定されたポートが使用されていないこと
- 同じASAサーバーがすでに登録されていないこと

### 3.3 SteamCMDによるインストール

- Palworld用に準備済みのSteamCMDがある場合は、それを再利用する。
- SteamCMDがない場合は、GSMの共通処理で準備する。
- ASAのSteam App IDを使用して専用サーバーをインストールする。
- PalworldとASAによるSteamCMDの同時実行を防止する。
- インストール失敗時は、画面に失敗理由を表示する。

### 3.4 初期設定

クライアントからログインして遊ぶために必要な設定ファイルを作成する。

- 対象マップを指定できること
- サーバー名が反映されること
- 最大プレイヤー数が反映されること
- サーバーパスワードが反映されること
- 管理者パスワードが反映されること
- 指定したポートが起動時に使用されること

最小構成では、ゲーム倍率や詳細ルールなどの高度な設定編集は必須としない。

### 3.5 Windows Firewall設定

- ASAへの接続に必要な受信規則を追加する。
- 構築画面で選択したネットワーク範囲だけを許可する。
- GSMが作成した規則だと識別できる名前を使用する。
- デモモードではWindows Firewallを変更しない。
- 構築失敗時は、その構築処理で追加した規則を可能な範囲で解除する。

### 3.6 サーバー起動

- ASAの実行ファイルと起動引数を正しく組み立てる。
- GSMから専用サーバープロセスを起動する。
- 起動直後にプロセスが終了した場合は、構築成功として扱わない。
- 起動中、停止中、失敗の状態を画面で確認できる。
- 長時間のインストールや起動確認中は、処理中であることを画面に表示する。

### 3.7 GSMへの登録

構築に成功したASAサーバーを、GSMの管理対象として保存する。

- ゲーム種別
- サーバー識別子
- サーバー名
- インストール先
- 使用ポート
- 接続を許可するネットワーク範囲
- 実サーバーまたはデモの区別
- 現在の状態

### 3.8 最低限の管理操作

構築後、GSMから次の操作ができる。

- 現在の起動状態を表示する
- 停止中のASAサーバーを起動する
- 起動中のASAサーバーを安全に停止する
- ASAサーバーを再起動する

停止時は、可能な限りASAの正式な保存・終了方法を使用し、ワールドデータを破損させない。

## 4. 最小構成に含めない機能

以下は、クライアントからログインして遊べることを確認した後の追加機能とする。

- すべてのゲーム設定項目を編集する機能
- MODの導入と管理
- 複数のASAサーバーやクラスタの管理
- ARK: Survival Evolvedへの対応
- サーバー更新の自動化
- 時刻指定による自動停止・自動起動
- プレイヤー一覧や管理者操作
- ログの高度な検索と分析
- GSM独自のワールドバックアップ
- インターネット越しの接続を自動構成する機能
- UPnPやルーターのポート転送設定

必要になった機能は、最小構成の完成後に個別の目標として追加する。

## 5. 接続範囲

最初の実機検証は、外部ルーターの設定を必要としない次のいずれかで行う。

- 同一LAN内からの接続
- Tailscale経由の接続

インターネットへ直接公開する場合に必要なルーターのポート転送、固定IP、CGNAT対策などは、GSMによる最小構成の対象外とする。

## 6. 完成条件

次の条件をすべて満たした時点で、ASA専用サーバー構築機能の最小構成を完成とする。

- GSMの構築画面から必要な情報を入力できる。
- 事前確認に成功してから構築処理が開始される。
- 共通のSteamCMDを使用してASA専用サーバーをインストールできる。
- 必要な初期設定ファイルが作成される。
- 必要なWindows Firewall規則が追加される。
- ASA専用サーバープロセスが正常に起動し、起動状態をGSMで確認できる。
- 別のPCまたはASAクライアントから対象サーバーを確認できる。
- 設定したパスワードでワールドへログインできる。
- プレイヤーキャラクターを作成し、ワールド内で移動や基本操作ができる。
- ログアウト後に再接続して、ワールドデータが保存されていることを確認できる。
- GSMから安全に停止し、再起動後も同じワールドへログインできる。
- 既存のPalworld構築・管理機能が引き続き動作する。

単にプロセスが起動しただけでは完成としない。ASAクライアントによるログイン、プレイ、保存、再接続までを実機で確認して完成とする。

## 7. 検証段階

実装と検証は次の順番で進める。

1. 単体テストで入力確認やコマンド生成を確認する。
2. デモモードで画面と構築処理の流れを確認する。
3. 開発マシンでSteamCMDによる実インストールを確認する。
4. サーバーマシンでWindows Firewall設定とプロセス起動を確認する。
5. 同一LANまたはTailscale上のASAクライアントから接続する。
6. ログイン、プレイ、保存、停止、再起動、再接続を確認する。
7. Palworldの主要機能に影響がないことを確認する。

## 8. 最初の調査

ASAの公式な専用サーバー仕様を基準として、Steam App ID、必要容量、実行ファイル、ポート、起動引数、設定ファイル、保存・終了方法を調査し、最初の実装タスクを決める。
