# パルワールド専用サーバー構築

## 目的

現在はゲーム用PCが協力プレイのホストを兼ねているため、ホストが不在のときはフレンドが遊べない。余っているPCを専用サーバー化し、3人が必要な時間に起動して遊べる環境を構築する。

## 採用する構成

今回は構成を単純にして問題を切り分けやすくするため、Windows上でPalworld Dedicated Serverを直接動かす。仮想マシン、WSL2、Docker Desktopは使用しない。将来ほかのオンラインサーバーを構築する場合は、Ubuntu Serverと公式Dockerイメージを検討する。

```text ゲーム用PC
Ryzen 7 9800X3Dクラス
Radeon RX 9070 XT
└─ Palworldクライアント

サーバー用PC
Core i5-11400クラス
メモリ16～32GB
SSD
Windows
├─ Palworld Dedicated Server
├─ Tailscale
├─ 遠隔管理
└─ 定期バックアップ
```

専用サーバーはGPUをほぼ使用しない。CPUがCore i5-11400であれば3人用として十分で、メモリは16GBでも開始できるが32GBが望ましい。RTX 3060 Tiは、CPU内蔵GPUとマザーボードの映像出力を利用できる場合は取り外しを検討する。

## 構築方針

ネットワークとセーブ移行を同時に進めない。最初に空の専用サーバーへ3人全員が接続できることを確認し、その後で既存ワールドを移行する。

```text
1. Windows初期化
2. Windows基本設定
3. LAN内リモート管理
4. Tailscale設定
5. 空の専用サーバー構築
6. 3人の接続確認
7. 3人の新プレイヤーID確認
8. 既存ワールドのバックアップ
9. ワールド移行
10. キャラクターID変換
11. データ確認
12. 自動起動・バックアップ・遠隔電源管理
```

## 1. Windowsを初期化する

初期化前に以下を確認する。

- `設定 → システム → ライセンス認証`で認証済みであること
- WindowsのエディションがHomeかProか記録すること
- 必要なデータを外部ストレージへ退避すること
- BitLockerが有効なら回復キーを保管すること

初期化は以下を選択する。

```text
設定
→ システム
→ 回復
→ PCをリセットする
→ すべて削除する
→ クラウドからダウンロード
```

同じPCへ同じWindowsエディションを再インストールする場合、通常はインターネット接続後にデジタルライセンスが自動認証される。自分で再利用するPCなので、売却時向けの「データのクリーニング」は原則不要。

## 2. Windowsの基本設定

初期化後は以下を実施する。

- Windows Updateを完了する
- ライセンス認証を確認する
- 分かりやすいPC名を設定する
- 管理用ユーザーに強固なパスワードを設定する
- スリープと休止を無効化する
- 有線LANでルーターへ接続する
- BIOSで停電復旧後の自動電源投入を検討する
- サーバーPCの吸排気口と壁の間を確保する

サーバーPCのLAN内IPは以下で確認する。

```powershell
ipconfig
```

ルーター側のDHCP固定割当機能を使い、サーバーPCへ同じLAN内IPが割り当てられるようにする。

## 3. LAN内から遠隔管理する

Windows Proの場合はリモートデスクトップを利用できる。

```text
設定
→ システム
→ リモートデスクトップ
→ 有効
```

ゲーム用PCで以下を起動し、サーバーPCのLAN内IPへ接続する。

```powershell
mstsc
```

Windows Homeの場合はリモートデスクトップの受信機能がないため、OpenSSH Serverなど別の管理方法を利用する。

## 4. Tailscaleを設定する

サーバーPC、ゲーム用PC、フレンド2人のPCへTailscaleを導入する。フレンドは各自のアカウントで同じtailnetへ参加する。

サーバーPCのTailscale IPを確認する。

```powershell
tailscale ip -4
```

接続先は以下の形式になる。

```text
100.x.x.x:8211
```

Tailscaleを利用する場合、原則としてルーターでUDP 8211をインターネットへポート開放しない。

## 5. Palworld Dedicated Serverを構築する

SteamCMDを利用して専用サーバーを導入する。

```powershell
steamcmd +login anonymous +app_update 2394010 validate +quit
```

標準の待受ポートはUDP 8211。起動オプションでポートを明示する場合は以下を使用する。

```text
-port=8211
```

初回は既存ワールドをコピーせず、空のワールドを生成する。

## 6. Windows Firewallを設定する

サーバーPCでUDP 8211の受信を許可する。

```powershell
New-NetFirewallRule `
  -DisplayName "Palworld Dedicated Server UDP 8211" `
  -Direction Inbound `
  -Protocol UDP `
  -LocalPort 8211 `
  -Action Allow `
  -Profile Any
```

サーバー起動中に待受状態を確認する。

```powershell
Get-NetUDPEndpoint -LocalPort 8211
```

結果が表示されなければ、PalworldサーバーがUDP 8211で待ち受けていない。

動作確認後は、必要に応じてWindows Firewallの適用範囲をTailscaleからの通信に限定する。

## 7. 接続を段階的に確認する

### LAN内接続

ゲーム用PCから、サーバーPCのLAN内IPへ接続する。

```text
192.168.x.x:8211
```

これで接続できれば、Palworld Dedicated Server自体は動作している。

### 自宅からTailscale接続

ゲーム用PCから疎通を確認する。

```powershell
tailscale ping 100.x.x.x
```

成功後、Palworldから以下へ接続する。

```text
100.x.x.x:8211
```

### フレンドからTailscale接続

フレンド2人のPCでも以下を実行する。

```powershell
tailscale ping 100.x.x.x
```

成功後、同じTailscale IPとポートへ接続する。3人全員が空のワールドへ接続できるまでは、既存ワールドをコピーしない。

### エラーの切り分け

| 症状 | 主な原因 |
|---|---|
| `tailscale ping`が失敗 | Tailscaleの招待、共有、アカウント、アクセス制御 |
| ping成功、ゲーム接続失敗 | Windows Firewall、UDP 8211、サーバー待受 |
| 新規キャラクター作成画面が出る | 通信は成功、キャラクターIDが未対応 |
| レベル1でワールドへ入る | 旧キャラクターとのID対応に失敗 |
| 他人のキャラクターになる | IDの対応付けが誤っている |

以前の失敗では、自分だけ接続でき、フレンドはサーバー接続エラーになった。キャラクター作成画面にも到達していないため、主原因はセーブデータではなくTailscaleまたはFirewallなどの通信設定だった可能性が高い。

## 8. 3人分の新プレイヤーIDを記録する

空の専用サーバーへ1人ずつ接続し、仮キャラクターを作成する。接続するたびに`Players`フォルダへ増えたファイル名を記録する。

```text
1. 自分だけ接続
2. 新しく生成されたIDを記録
3. フレンド1だけ接続
4. 新しく生成されたIDを記録
5. フレンド2だけ接続
6. 新しく生成されたIDを記録
```

対応表を作成する。

| プレイヤー | 既存ワールドの旧ID | 空サーバーの新ID |
|---|---|---|
| 自分 | `00000000000000000000000000000001` | 確認後に記録 |
| フレンド1 | 確認後に記録 | 確認後に記録 |
| フレンド2 | 確認後に記録 | 確認後に記録 |

元ホストは協力プレイ用の特殊IDを使用するため、原則として変換が必要。フレンドは旧IDと新IDが同じなら変換不要で、異なる場合は変換する。

## 9. 既存ワールドをバックアップする

ゲーム用PCのセーブデータは通常以下にある。

```text
%LOCALAPPDATA%\Pal\Saved\SaveGames\
```

対象ワールドのフォルダを丸ごと外付けSSDや別PCへコピーする。さらにZIP化した原本を1つ残し、変換ツールやサーバーで直接使用しない。

主なファイル構成は以下。

```text
<ワールドID>
├─ Level.sav
├─ LevelMeta.sav
├─ LocalData.sav
├─ WorldOption.sav
└─ Players
   ├─ 00000000000000000000000000000001.sav
   └─ その他のプレイヤーID.sav
```

移行作業を開始した後は、元の協力プレイワールドで遊ばない。移行中に元データが更新されると、どちらが最新版か判断できなくなる。

## 10. 既存ワールドをサーバーへ移行する

1. Palworld Dedicated Serverを正常に停止する
2. 空の専用サーバーワールドをバックアップする
3. 既存ワールドフォルダをサーバーの`SaveGames\0\`配下へコピーする
4. `GameUserSettings.ini`の`DedicatedServerName`を既存ワールドのフォルダ名へ合わせる
5. 元ホストと、IDが変わったフレンドのキャラクターを新IDへ対応付ける
6. 必要に応じてギルド・拠点情報も修正する
7. サーバーを起動して確認する

稼働中のサーバーへセーブファイルを上書きしない。Pocketpair公式の包括的な移行ツールはないため、キャラクターID変換でコミュニティ製ツールを利用する場合は、必ずバックアップのコピーに対して実行する。

## 11. 移行後の確認項目

3人全員で以下を確認する。

- キャラクター名と外見
- レベルとステータス
- 所持品と装備
- 手持ちパル
- パルボックス
- テクノロジー解放状況
- ファストトラベル
- ギルド所属
- 拠点の所有権
- 建築物と保管箱
- マップ進捗

問題があれば、編集済みデータを継ぎ足して修正せず、変更していないバックアップからやり直す。

## 12. 自動起動とバックアップ

安定稼働後に以下を設定する。

- Windows起動時にTailscaleを自動起動
- Windows起動時にPalworld Dedicated Serverを自動起動
- サーバー停止前にワールドを保存
- 日次でセーブデータをバックアップ
- サーバー更新前にもバックアップ
- バックアップを別ディスクまたは別PCにも保存

サーバー更新コマンドは以下。

```powershell
steamcmd +login anonymous +app_update 2394010 validate +quit
```

更新前にサーバーを停止し、セーブデータをバックアップする。

## 13. 遠隔電源管理

サーバーPCを使用しない時間は停止し、外出先から必要なときだけ起動できる構成を目指す。

```text
起動
iPhone
→ Wake-on-LAN中継
→ サーバーPC起動
→ TailscaleとPalworldを自動起動

停止
iPhone
→ Tailscale経由で停止指示
→ ワールド保存
→ Palworld正常終了
→ バックアップ
→ Windowsシャットダウン
```

Wake-on-LANは起動専用であり、停止機能はない。Windowsの停止はリモートデスクトップ、SSH、または管理用スクリプトから安全に実行する。

```powershell
shutdown /s /t 60
```

外出先からのWake-on-LANは、ルーターに安全な遠隔WoL機能があるか最初に確認する。なければRaspberry Pi Zero 2 Wなどの常時稼働中継機器、またはスマートプラグとBIOSの通電時自動起動を検討する。スマートプラグで稼働中のPCを強制的に切らない。

## 参考情報

- Palworldサーバー要件: https://docs.palworldgame.com/ja/0.5.0/getting-started/requirements/
- Palworld専用サーバー構築: https://docs.palworldgame.com/0.5.0/getting-started/deploy-dedicated-server/
- Palworld起動引数: https://docs.palworldgame.com/settings-and-operation/arguments/
- Tailscale端末接続: https://tailscale.com/kb/1452/connect-to-devices
- Windowsの初期化: https://support.microsoft.com/en-us/windows/reset-your-pc-0ef73740-b927-549b-b7c9-e6f2b48d275e
